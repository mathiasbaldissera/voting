spring:
  datasource:
    url: "jdbc:h2:mem:db;DB_CLOSE_DELAY=-1"
    username: sa
  flyway:
    user: sa
    url: "jdbc:h2:mem:db;DB_CLOSE_DELAY=-1"
    baseline-on-migrate: true
    schemas: PUBLIC
